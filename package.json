{
  "name": "project-with-harness",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm -F frontend dev\" \"pnpm -F backend dev\"",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
